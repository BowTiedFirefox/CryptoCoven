{
  "name": "CryptoCoven",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bowtiedfirefox/CryptoCoven",
    "create-local": "graph create --node http://localhost:8020/ bowtiedfirefox/CryptoCoven",
    "remove-local": "graph remove --node http://localhost:8020/ bowtiedfirefox/CryptoCoven",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bowtiedfirefox/CryptoCoven"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
